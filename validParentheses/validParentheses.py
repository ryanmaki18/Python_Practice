class Solution:
    def isValid(self, s: str) -> bool:
        valid_chars = {'(', ')', '{', '}', '[', ']'}
    #    input_order

        if 1 <= s.length <= 10**4
            for char in s:
                if char not in valid_chars:
                    return False
            
            return True
    
        else: 
            return False